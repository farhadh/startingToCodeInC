#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#include <stdlib.h>
#include <time.h>

int main() {

	double x = 25;
	double y = 35;

	x > y ? printf("yes; x > y\n"): printf("no; y > x\n");

	/* <condition>  ? <if block of the code> : <else if part> ; */

	return 0;
}
